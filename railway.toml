[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn 'src.web:app' --bind '0.0.0.0:$PORT' --workers 2 --timeout 60"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
